name: Build Firmware

on:
  push:
    branches: [ "refactor/phase1-safety" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Build firmware
        run: pio run

      - name: Rename merged binary
        run: mv .pio/build/esp32dev/merged-flash.bin IH-v1.1.0-CT-PLL.bin

      - name: Upload firmware binary
        uses: actions/upload-artifact@v4
        with:
          name: IH-firmware
          path: IH-v1.1.0-CT-PLL.bin
